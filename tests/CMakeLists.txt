# Tests CMakeLists.txt
# Tests are disabled in Phase 1
# TODO: Add test files when ready

# Enable testing
# enable_testing()

# Test executable (disabled for now)
# add_executable(mcpp_tests
#     unit/json_rpc_message_test.cpp
#     unit/stdio_transport_test.cpp
#     unit/protocol_test.cpp
#     integration/client_server_test.cpp
# )

# target_link_libraries(mcpp_tests PRIVATE mcpp)

# Add test cases (disabled for now)
# add_test(NAME UnitTests COMMAND mcpp_tests)